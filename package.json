{
  "name": "chatgpt-code-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "gateway:dev": "pnpm --filter @chatgpt-code-mcp/gateway dev",
    "gateway:deploy": "pnpm --filter @chatgpt-code-mcp/gateway deploy",
    "agent": "pnpm --filter @chatgpt-code-mcp/agent start"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
